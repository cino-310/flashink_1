import React from "react";

import { Toolbartitle } from '../ui-components';

const ToolBarTitle = () => {
    return (
        <Toolbartitle overrides={{
            Toolbar: {
                // character
            }
        }} />
    )
};

export default ToolBarTitle